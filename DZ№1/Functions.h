#ifndef Functions
#define Functions
#endif

#include <stdio.h>

double dispersion(char* wordptr ,char* docnameptr);
int sentencecounter (FILE* file);
int wordcounter(int* arrname, int size, char* wordptr, FILE* file);
double discounter(int* arrname, int size);

